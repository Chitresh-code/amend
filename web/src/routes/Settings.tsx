export function Settings() {
  return (
    <div style={{ padding: 48, maxWidth: 640, margin: '0 auto' }}>
      <h1 style={{ fontSize: 22, marginBottom: 8 }}>Settings</h1>
      <p style={{ color: 'var(--text-muted)', fontSize: 14 }}>
        Model credentials, API keys, account, and retrieval settings (Epics D through G) are not
        yet implemented.
      </p>
    </div>
  );
}
